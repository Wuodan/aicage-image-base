name: Build Base Image (Fedora)

"on":
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  build:
    uses: ./.github/workflows/base-image.yml
    with:
      base: fedora
    secrets: inherit
