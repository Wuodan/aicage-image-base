name: Build Base Image (Node)

"on":
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  build:
    uses: ./.github/workflows/base-image.yml
    with:
      base: node
    secrets: inherit
